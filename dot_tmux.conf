unbind C-b
set -g prefix C-Space
set -g mouse on
set-window-option -g automatic-rename off
set-option -sa terminal-overrides ",xterm*:Tc"

unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"

unbind v
unbind h

unbind % # Split vertically
unbind '"' # Split horizontally

bind v split-window -v -c "#{pane_current_path}"
bind h split-window -h -c "#{pane_current_path}"

unbind ö
bind ö rename-window "backend" \; split-window -v -c "#{pane_current_path}" \; resize-pane -y 20% \; new-window -n "frontend" \; select-window -t "frontend" \; split-window -v -c "#{pane_current_path}" \; resize-pane -y 20% \; new-window -n "terminal" \; select-window -t "backend" 
bind + split-window -v -c "#{pane_current_path}" \; resize-pane -y 20%

bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# bind ö rename-window "terminal" \; new-window -n "backend" \; split-window -h -c "#{pane_current_path}" \; new-window -n "frontend" \; split-window -h -c "#{pane_current_path}"

# bind-key -r -T prefix C-h resize-pane -L
# bind-key -r -T prefix C-j resize-pane -D
# bind-key -r -T prefix C-k resize-pane -U
# bind-key -r -T prefix C-l resize-pane -R

set -g history-limit 10000

set -g base-index 1
set-window-option -g pane-base-index 1


##############
### DESIGN ###
##############

# # panes
# set -g pane-border-style fg=black
# set -g pane-active-border-style fg=red
#
# ## Status bar design
# # status line
# set -g status-justify centre
# #set -g status-bg default
# set -g status-style fg=blue
# set -g status-interval 2
#
# # messaging
# set -g message-command-style fg=blue,bg=black
#
# # window mode
# setw -g mode-style bg=green,fg=black
#
# # window status
# setw -g window-status-format " #F#I:#W#F "
# setw -g window-status-current-format " #F#I:#W#F "
# setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=white] #W "
# setw -g window-status-current-format "#[bg=brightmagenta]#[fg=white] #I #[fg=white]#[bg=cyan] #W "
# setw -g window-status-current-style bg=black,fg=yellow
# setw -g window-status-style bg=green,fg=black,reverse
#
# # loud or quiet?
# set -g visual-activity on
# set -g visual-bell on
# set -g visual-silence off
# set-window-option -g monitor-activity off
# set -g bell-action none
#
# # The modes 
# set-window-option -g clock-mode-colour red
# set-window-option -g mode-style fg=red,bg=black,bold
#
# # The panes 
# set -g pane-border-style bg=black,fg=red
# set -g pane-active-border-style fg=green,bg=black
#
# # The statusbar 
# set -g status-position bottom
# set -g status-style bg=black,fg=yellow
# set -g status-right ''
# set-option -ag status-right "#[fg=red,bg=default]#(uptime | cut -f 4-5 -d ' ' | cut -f 1 -d ',') "
# set-option -ag status-right " #[fg=white,bg=default]%a%l:%M:%S %p#[default] #[fg=blue]%Y-%m-%d"
# set -g status-left '#{?client_prefix,#[fg=white]#[bg=red]#[bold] - PREFIX - ,#[fg=brightwhite]#H}'
#
# set -g status-right-length 50
# set -g status-left-length 20
#
# # The window
# set-window-option -g window-status-current-style fg=red,bg=black,bold
# set-window-option -g window-status-current-format ' #I#[fg=brightwhite]:#[fg=brightwhite]#W '
#
# set-window-option -g window-status-style fg=magenta,bg=black,none
# set-window-option -g window-status-format ' #I#[fg=brightblue]:#[fg=brightblue]#W#[fg=black]#F '
#
# set-window-option -g window-status-bell-style fg=white,bg=red,bold
#
# # The messages 
# set -g message-style fg=white,bg=red,bold
#

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
set -g @plugin 'tmux-plugins/tmux-yank'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
